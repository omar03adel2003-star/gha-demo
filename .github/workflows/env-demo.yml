name: Env Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"  # Fixed: changed App_ENV to APP_ENV
jobs:
  env_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to Env
        run: echo "App is connecting to $APP_ENV"
