name: Env Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  build:  # Changed the name to match your latest screenshot
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4

      - name: Connecting to Env  # This is the step you are looking for
        run: echo "App is connecting to $APP_ENV"

      - name: Print index.html file
        run: cat index.html
